# PlaJA Compile

Compile PlaJA inside the dependency Docker image used by the experiments.

```bash
./run-experiments/PLAJA-COMPILE/compile_plaja.sh /path/to/plaja
```

If no path is provided, the script prompts for the PlaJA directory. The binary is written to `<plaja>/build/PlaJA`.
